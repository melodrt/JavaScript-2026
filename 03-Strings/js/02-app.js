const producto = "Monitor 20 Pulgadas";

console.log(producto);
console.log(producto.length); // Muestra la cantidad de caracteres de la cadena

console.log(producto.indexOf("Pulgadas")); // Muestra la posición de la palabra "Pulgadas" en la cadena

console.log(producto.includes("Monitor")); // Devuelve true si la palabra "Monitor" se encuentra en la cadena, de lo contrario devuelve false

