const producto = "Monitor 20 Pulgadas";
console.log(producto.toUpperCase()); // Convierte a mayúsculas
console.log(producto.toLowerCase()); // Convierte a minúsculas

const email = "CORREO@CORREO.COM";
console.log(email.toLowerCase()); // Convierte a minúsculas

const precio = 300;
console.log(precio); // Muestra el número
console.log(precio.toString()); // Convierte a string
